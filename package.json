{
  "name": "psn-bibliothek",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node serve.js",
    "test": "jest",
    "lint": "eslint . --ext .js",
    "ci": "npm run lint && npm test"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "jest": "^29.0.0"
  }
}
